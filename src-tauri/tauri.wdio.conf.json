{
  "$schema": "https://schema.tauri.app/config/2",
  "app": {
    "security": {
      "capabilities": [
        "main",
        {
          "identifier": "wdio",
          "description": "TEST ONLY. Grants the WebDriver plugin's commands so the acceptance suite can drive the real window. Declared inline in a config the published build never loads, because an app shipping this would let anything on the machine drive it.",
          "windows": ["main"],
          "permissions": ["wdio:default"]
        }
      ]
    }
  }
}
