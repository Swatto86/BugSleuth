{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Least privilege for the main window. Everything BugSleuth does that touches the filesystem or spawns a process happens in Rust behind an explicit command, so the frontend needs no fs or shell permission at all \u2014 only the window controls it actually calls, plus a native folder picker.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-theme",
    "core:window:allow-theme",
    "core:event:allow-listen",
    "dialog:allow-open"
  ]
}
